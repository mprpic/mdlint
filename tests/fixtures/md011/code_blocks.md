# Code Blocks

Reversed link syntax in code blocks should be ignored:

```markdown
(This is an example)[https://example.com/]
```

    (Indented code block)[https://example.com/]

And inline code: `(text)[link]` should also be ignored.
